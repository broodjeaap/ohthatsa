kind: pipeline
type: docker
name: default

steps:
  - name: test
    image: cirrusci/flutter:stable
    commands:
      - flutter pub get
      - flutter pub run test
trigger:
  branch:
    - master
  event:
    - push